workflows:
  - name: download
    provider: python
    requirements: requirements.txt
    script: mnist/prepare_data.py
    artifacts:
      - data

  - name: train
    provider: python
    requirements: requirements.txt
    script: mnist/train.py
    depends-on:
      - download
    artifacts:
      - model
    resources:
      gpu: 1
