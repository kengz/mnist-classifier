datamodule:
  batch_size: 32
model:
  hidden_size: 64
  learning_rate: 2e-4
trainer:
  precision: 32
  accelerator: auto # will switch to GPU if available
  max_epochs: 10 # set to None to auto-stop on convergence
  enable_progress_bar: true
resume: false # resume training from the latest checkpoint
